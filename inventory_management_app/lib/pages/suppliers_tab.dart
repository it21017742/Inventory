import 'package:flutter/material.dart';

class SuppliersTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Suppliers Screen', style: TextStyle(fontSize: 24)),
    );
  }
}
