import 'package:flutter/material.dart';

class ItemsTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Items Screen', style: TextStyle(fontSize: 24)));
  }
}
